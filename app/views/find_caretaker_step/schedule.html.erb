<div class="page-wrapper displayCare">
  <div class="body no-padding sign-up">
    <div class='sign-up-banner'>
      <div class="overlay"></div>
      <div class="row">
        <div class="family-sign-up-box">
          <h4>Tell us about the care schedule</h4>
          <p>When would you like care to begin?</p>

          <%= form_tag(find_caretaker_step_index_path(id: @contract.id, next_step: :caretaker), method: :put) do %>

              <div class="field full-width">
                <%= text_field_tag :expected_date, nil, id: 'datepicker' %>
                <i class="fi-calendar"></i>
              </div>
              <br>
              <br>
              <p>Is your weekly care schedule flexible?</p>

              <div class="field">
                <%= label_tag 'Flexible schedule' %>
                <%= radio_button_tag 'schedule_type', '0' %>
              </div>

              <div class="field">
                <%= label_tag 'Set schedule for the week' %>
                <%= radio_button_tag 'schedule_type', '1' %>
              </div>

              <div id="flexible">
                <div class="field full-width">
                  <%= label_tag 'How many days of care are needed each week?' %>
                  <%= text_field_tag :days_of_care, nil, id: 'days_of_care' %> days
                </div>

                <p>What time(s) of day is care needed?</p>

                <div class="field full-width">
                  <% time_of_care.each do |t| %>
                      <%= label_tag t[0], nil, class: 'week-label' %>
                      <%= check_box_tag :time_of_care, t[1] %>
                  <% end %>
                </div>
              </div>

              <div id="weekly">
                <p>Please specify when you need care</p>

                <% day_of_week.each do |t| %>
                    <%= label_tag t[0], nil, class: 'week-label' %>
                    <%= check_box_tag :day, t[1] %>
                <% end %>

                <span id="week_timing">
                </span>
              </div>

              <div class="field full-width">
                <%= label_tag 'How many hours per week of care are required?' %>
                <%= text_field_tag :hours_needed, nil, id: 'hours_needed' %> hours/week
              </div>

              <%= hidden_field_tag :schedule_time %>

              <div class="action">
                <%= submit_tag 'Next', class: 'btn blue absolute schedule_step' %>
              </div>

          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
